puts "On va compter le nombre d'heures de travail a THP" #cette ligne nous dit que l'on va compter le nombre d'heure de travail a thp en nous affichant le text comme tel
puts "Travail : #{10 * 5 * 11}" # cette ligne  sert à afficher "Travail" + une instruction en commentaire qui represente 10 heure 5 jours et 11 semaines convertit en heures
puts "En minutes ca fait : #{10 * 5 * 11 * 60}" # cette ligne nous indique qu'elle va convertir les heure de travaille en minute à trravers un text et nous donner le resultat à travers une opération

puts "Et en secondes ?" # cette ligne nous indique une question qui est de savoir la conversion du résultat précédents en secondes

puts 10 * 5 * 11 * 60 * 60 # cette ligne nous indique directement le resultat de la question en effectuant une opérration

puts "Est-ce que c'est vrai que 3 + 2 < 5 - 7 ?" # cette ligne nous indique de 2 opération qui sont mis dans un ordre croissant tout en posant une question 

puts 3 + 2 < 5 - 7 # cette ligne  nous indique que c'est faux en effectuant l'opération liée à la question précédente 

puts "Ca fait combien 3 + 2 ? #{3 + 2}" # cette ligne nous indique la valeur de la 1ére opération dans l'opération préccédentes sous forme de question reponse
puts "Ca fait combien 5 - 7 ? #{5 - 7}" # cette ligne  nous indique la valeur  de la 2éme opération sous forme de question reponse

puts "Ok, c'est faux alors !" # cette ligne nous indique que l'opération  était vraisemblabement fausse

puts "C'est drole ca, faisons-en plus :" # cette ligne nous indique que l'on va effectuer d'autre operation simiilaires

puts "Est-ce que 5 est plus grand que -2 ? #{5 > -2}" # cette ligne nous indique  2 opération qui sont mis dans un ordre décroissant sous fome de question tout en donnant la reponse
puts "Est-ce que 5 est superieur ou egal a -2 ? #{5 >= -2}" # cette ligne nous indique  2 opération qui sont mis dans un ordre décroissant ou egal sous fome de question tout en donnant la reponse
puts "Est-ce que 5 est inferieur ou egal a -2 ? #{5 <= -2}"  # cette ligne nous indique  2 opération qui sont mis dans un ordre croissant ou egal sous fome de question tout en donnant la reponse
